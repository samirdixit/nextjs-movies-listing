This repo contains the code for a basic movies listing portal built using NextJS.

Note: Before running the project, create a .env file using example.env file and replace the dummy values with actual data.
